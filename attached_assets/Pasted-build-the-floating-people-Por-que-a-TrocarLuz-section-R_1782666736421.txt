build the floating-people "Por que a TrocarLuz?" section:

Rebuild the "Por que a TrocarLuz?" section as a floating-photo collage like a premium animated site, replacing the scattered 3D objects currently there. Report files touched, then implement. Do not report done until tiles float with visible multi-axis motion and nothing overlaps the heading.
LAYOUT:

Keep the section background pale yellow (#F4EAA6) with the existing dot texture. Center the heading "Por que a TrocarLuz?" (Poppins, bold, dark ink #1A2410). REMOVE the large ghost word "Economia" from this section — the floating photos replace it (keep ghost words only on hero/CTA).
Add a relative container. Around the heading, absolutely-position 7 squircle photo tiles using /public/img/person-1, -3, -4, -6, -7, -8, -10.webp, plus 2 small 3D objects (/public/img/obj-raio.webp and obj-fazenda.webp). Tiles vary 120–200px, objects ~90px, each with slight rotation (−6° to +6°) and a soft shadow (0 18px 40px rgba(0,0,0,.12)), border-radius ~22%.
CLEAR CENTRAL ZONE: no tile or object may enter a ~640px-wide × 220px-tall safe box around the heading, nor come within 60px of the heading text. Tiles live in the outer ring only (corners + left/right edges).
Each tile img: loading="lazy", explicit width/height (no layout shift), alt="" (decorative), object-fit cover.
Tablet (≤1024px): show 5 tiles + 1 object. Mobile (≤768px): show 4 tiles only, 96–120px, one in each corner of the section, heading centered between them — verify none overlaps the heading text.

MOTION (framer-motion; float on the <img>, parallax on the wrapper div — never both on the same element):

PHOTO TILES = moderate continuous float, each a motion.img:

animate={{ y:[0,-14,0,-8,0], x:[0,6,0,-5,0], rotate:[r-2,r+2.5,r-1,r+1.5,r-2], scale:[1,1.03,1,1.02,1] }} where r is the tile's base rotation; transition={{ duration: 6–8 (vary per tile), ease:"easeInOut", repeat:Infinity }}, plus a random delay 0–2s per tile so they're out of sync.
3D OBJECTS = energetic float: animate={{ y:[0,-22,0,-12,0], x:[0,10,0,-6,0], rotate:[-5,5,-3,4,-5], scale:[1,1.05,1,1.03,1] }}, transition duration 7s, repeat Infinity.
Wrap every tile/object in a motion.div with scroll parallax: const {scrollYProgress}=useScroll({target:ref,offset:["start end","end start"]}); const y=useTransform(scrollYProgress,[0,1],[Vstart,Vend]); give each a different range (e.g. [40,-40], [20,-20], [55,-25]) for depth.
useReducedMotion() disables both the float and parallax (tiles sit static).

VERIFY: confirm tiles use motion.img with the multi-axis values above; confirm parallax is on wrapper divs only; confirm the heading safe-zone has no overlapping tiles at desktop, tablet AND mobile; describe the visible motion. If any item isn't done, say which.
ACCEPTANCE: heading centered and never overlapped; 7 tiles + 2 objects on desktop in the outer ring; tiles visibly float on multiple axes out of sync AND parallax on scroll; reduced-motion safe; mobile shows 4 corner tiles with no heading overlap; lazy-loaded, no CLS, no horizontal scroll at 320px.
Second, the imagery swaps (now that obj-mercado exists) plus the global motion upgrade:
REPLIT PROMPT — imagery swaps + dynamic motion site-wide:

Two changes. Report files touched, then implement both.
PART A — swap card imagery (solar panels imply rooftop installation, which contradicts our "no installation" message):

"até 30% off / Mercado Livre" card: use /public/img/obj-mercado.webp (contract + toggle). Remove obj-solar here.
"até 35% off / Geração Distribuída" card: use /public/img/hero-casa.webp (energy bill + green down-arrow). Remove obj-solar here.
Remove obj-solar (the solar panel) from every card/decorative use site-wide. Keep obj-fazenda (solar farm) ONLY in the "Como funciona" section.
Re-verify no image overlaps text after swapping.

PART B — make ALL existing 3D render floats dynamic (current motion is a barely-visible ~10px bob). Keep framer-motion motion.img, keep parallax on wrappers, keep reduced-motion gating. Replace each render's animate/transition with one of three larger multi-axis variants, assigned by index (A,B,C,A…), each with a random 0–2s delay:

A: animate={{ y:[0,-22,0,-12,0], x:[0,8,0,-6,0], rotate:[-3,4,-2,3,-3], scale:[1,1.04,1,1.02,1] }} transition duration 7s repeat Infinity

B: animate={{ y:[0,18,0,24,0], x:[0,-10,0,6,0], rotate:[2,-5,3,-3,2], scale:[1,1.03,1,1.05,1] }} duration 8.5s

C: animate={{ y:[0,-16,0], rotate:[-6,6,-6], scale:[1,1.05,1] }} duration 6s

Use the energetic ones (A/B) on the hero lightning. Amplitudes are intentionally large — do not reduce.
VERIFY: confirm each render visibly moves on several axes within ~2s; confirm Mercado Livre uses obj-mercado, GD uses hero-casa, and no solar panel remains except the farm in "Como funciona". Say if anything was skipped.